# Local data volumes (database and uploads)
/data/
/backend/data/
docs-private/

# Local npm cache
/frontend/.npm-cache/

# Local env files (do not commit secrets)
.env
/backend/.env

# Local config directory (user-specific, not tracked)
config-local/


test/